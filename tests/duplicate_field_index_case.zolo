use orm::Model

@derive(Model)
struct Item {
  @model(primary_key: true)
  id: int,
  @model(index: true, column: "upper_code")
  Code: str,
  @model(index: true, column: "lower_code")
  code: str,
}
