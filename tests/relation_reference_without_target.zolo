use orm::Model
@derive(Model)
struct Parent {
  @model(primary_key: true)
  id: int,
}
@derive(Model)
struct Child {
  @model(primary_key: true)
  id: int,
  @model(references: Parent.id)
  parent_id: int,
}
