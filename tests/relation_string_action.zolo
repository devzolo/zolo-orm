use orm::Model
@derive(Model)
struct Parent {
  @model(primary_key: true)
  id: int,
}
@derive(Model)
struct Child {
  @model(primary_key: true)
  id: int,
  @model(belongs_to: Parent, foreign_key: true, on_delete: "cascade")
  parent_id: int,
}
