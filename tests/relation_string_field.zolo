use orm::Model
@derive(Model)
struct Parent {
  @model(primary_key: true)
  id: int,
}
@derive(Model)
struct Child {
  @model(primary_key: true)
  id: int,
  @model(belongs_to: Parent, references: "id")
  parent_id: int,
}
