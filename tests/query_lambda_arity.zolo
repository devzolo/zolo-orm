use orm::Query
use dx_fixture::{Item, Team}
@derive(Query)
struct View {
 item: Item,
 @join(from: View.item, key: Item.id, references: Team.id)
 team: Team?,
}
let query = View::query().filter(|item| item.id > 0)
