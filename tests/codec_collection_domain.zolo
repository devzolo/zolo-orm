use orm::Model
struct Codec {}
impl Codec {
  fn encode(value: [str]) -> str { value.join(",") }
  fn decode(value: str) -> Result<[str], str> { Result::Ok([value]) }
}
@derive(Model)
struct Item {
  @model(primary_key: true)
  id: int,
  @model(codec: Codec)
  tags: [str],
}
