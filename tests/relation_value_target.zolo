use orm::Model
@derive(Model)
struct Parent {
  @model(primary_key: true)
  id: int,
}
let instance = Parent { id: 1 }
@derive(Model)
struct Child {
  @model(primary_key: true)
  id: int,
  @model(belongs_to: instance)
  parent_id: int,
}
