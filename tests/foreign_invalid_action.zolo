use orm::Model

@derive(Model)
struct Parent {
  @model(primary_key: true)
  id: int,
  code: str,
}

@derive(Model)
struct Item {
  @model(primary_key: true)
  id: int,
  @model(belongs_to: "Parent", foreign_key: true, on_delete: "remove")
  owner_id: int,
}
