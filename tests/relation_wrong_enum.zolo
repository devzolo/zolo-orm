use orm::Model
@derive(Model)
struct Parent {
  @model(primary_key: true)
  id: int,
}
enum OtherAction { Cascade, }
@derive(Model)
struct Child {
  @model(primary_key: true)
  id: int,
  @model(belongs_to: Parent, foreign_key: true, on_delete: OtherAction::Cascade)
  parent_id: int,
}
